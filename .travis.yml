language: python
python:
  - "3.8"

branches:
  only:
    - main

install:
  - python -m pip install --upgrade setuptools==70.0.0
  - python -m pip install -r requirements.txt
  - python -m pip install black flake8 coverage coveralls

before_script:
  - python -m black .
  - flake8 --extend-exclude .venv,eb-virt,.ebextensions,.elasticbeanstalk --max-line-length 119
  - python -m pip install urllib3==1.26.6



script:
  - coverage run -m pytest
  - coverage report

deploy:
  provider: elasticbeanstalk
  region: "us-east-1"
  app: "sweapp1"        
  env: "swe1-app-env"             
  bucket_name: "elasticbeanstalk-us-east-1-095179586850"  
  access_key_id: $AWS_ACCESS_KEY_ID
  secret_access_key: $AWS_SECRET_ACCESS_KEY
  on:
    branch: main

after_success:
  - coveralls